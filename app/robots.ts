import type { MetadataRoute } from "next";

export default function robots(): MetadataRoute.Robots {
  return {
    rules: {
      userAgent: "*",
      allow: "/",
    },
    sitemap: "https://revlyn.io/sitemap.xml", // TODO: replace with real production domain
  };
}
